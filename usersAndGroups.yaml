
Resources:
  UserPeter:
    Type: "AWS::IAM::User"
    Properties:
      Groups:
        - "Band"
      UserName: "Peter"
  UserPaul:
    Type: "AWS::IAM::User"
    Properties:
      Groups:
        - "Band"
      UserName: "Paul"
  UserMary:
    Type: "AWS::IAM::User"
    Properties:
      Groups:
        - "Band"
      UserName: "Mary"
  UserJohnny:
    Type: "AWS::IAM::User"
    Properties:
      Groups:
        - "Band"
      UserName: "Johnny"

  GroupBand:
    Type: "AWS::IAM::Group"
    Properties:
      GroupName: "Band"

